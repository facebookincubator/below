[package]
name = "procfs"
edition = "2018"
version = "0.1.0"
authors = ['Facebook']
license = "Apache 2.0"
include = ["src/lib.rs"]

[dependencies]
procfs-thrift = { path = "if" }
backtrace = "0.3"
lazy_static = "1.0"
libc = "0.2"
thiserror = "1.0"

[dev-dependencies]
tempfile = "3.1"
