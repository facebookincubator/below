# @generated by autocargo from //resctl/below/config:below-config

[package]
name = "below-config"
version = "0.11.0"
authors = ["Meta Platforms, Inc. and affiliates"]
edition = "2024"
description = "Configurations for below"
repository = "https://github.com/facebookincubator/below"
license = "Apache-2.0"

[dependencies]
anyhow = "1.0.98"
btrfs = { package = "below-btrfs", version = "0.11.0", path = "../btrfs" }
cgroupfs = { version = "0.11.0", path = "../cgroupfs" }
serde = { version = "1.0.219", features = ["derive", "rc"] }
toml = { version = "0.9.8", features = ["preserve_order"] }

[dev-dependencies]
tempfile = "3.22"
