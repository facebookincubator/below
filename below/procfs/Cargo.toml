# @generated by autocargo from //resctl/below/procfs:fb_procfs

[package]
name = "fb_procfs"
version = "0.10.0"
authors = ["Meta Platforms, Inc. and affiliates"]
edition = "2024"
description = "A crate for reading procfs"
readme = "README"
repository = "https://github.com/facebookincubator/below"
license = "Apache-2.0"

[dependencies]
common = { package = "below-common", version = "0.10.0", path = "../common" }
lazy_static = "1.5"
libc = "0.2.139"
nix = { version = "0.30.1", features = ["dir", "event", "hostname", "inotify", "ioctl", "mman", "mount", "net", "poll", "ptrace", "reboot", "resource", "sched", "signal", "term", "time", "user", "zerocopy"] }
openat = "0.1.21"
parking_lot = { version = "0.12.1", features = ["send_guard"] }
serde = { version = "1.0.219", features = ["derive", "rc"] }
slog = { version = "2.7", features = ["max_level_trace", "nested-values"] }
thiserror = "2.0.12"
threadpool = "1.8.1"

[dev-dependencies]
slog-term = "2.8"
tempfile = "3.15"
